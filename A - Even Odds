#include <cstdio>
#include <iostream>


using namespace std;

int main()
{
    long long n,k;
    cin>>n>>k;
    long long half = n - n/2;
    
    long long answer;
    if(k<=half)
    answer = 2*k-1;
    else
    answer = 2*(k-half);
    cout << answer;
}
